apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-flash-cards.fullname" . }}
  labels:
    {{- include "ai-flash-cards.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-flash-cards.selectorLabels" . | nindent 4 }}
